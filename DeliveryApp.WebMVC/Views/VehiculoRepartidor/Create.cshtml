@model DeliveryManagement.VehiculoRepartidor

@{
    ViewData["Title"] = "Asignar Vehículo a Repartidor";
}

<div class="min-vh-100 py-5" style="background: linear-gradient(rgba(255, 255, 255, 0.9), rgba(255, 255, 255, 0.9)), url('https://thumbs.dreamstime.com/z/nahtlose-muster-der-pizza-mit-zutaten-und-lieferungen-gezogene-%C3%BCbergabe-nahrungsmittelgekritzel-isoliert-auf-wei%C3%9Fem-hintergrund-191909444.jpg'); background-size: cover;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-lg-6">
                <div class="card shadow-lg rounded-4">
                    <div class="card-body p-4">
                        <h1 class="h3 text-primary mb-4 text-center">
                            <i class="bi bi-truck me-2"></i> Asignar Vehículo a Repartidor
                        </h1>
                        <form asp-action="Create">
                            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                            <div class="mb-3">
                                <label asp-for="RepartidorId" class="form-label fw-bold"></label>
                                <input asp-for="RepartidorId" class="form-control" />
                                <span asp-validation-for="RepartidorId" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <label asp-for="VehiculoId" class="form-label fw-bold"></label>
                                <input asp-for="VehiculoId" class="form-control" />
                                <span asp-validation-for="VehiculoId" class="text-danger"></span>
                            </div>

                            <div class="d-flex justify-content-between">
                                <a asp-action="Index" class="btn btn-outline-secondary">
                                    <i class="bi bi-arrow-left-circle me-1"></i> Volver
                                </a>
                                <button type="submit" class="btn btn-primary">
                                    <i class="bi bi-check-circle me-1"></i> Asignar
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
